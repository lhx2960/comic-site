# 漫画站项目协作约定

> 任何新会话、子代理开工前先读本文件；角色细节见 `docs/roles/`。

## 项目状态

- 阶段：**立项中** —— 范围已定稿，需求（PRD）与设计未定稿，尚未创建 Next.js 工程
- 技术栈：Next.js（App Router，前后端一体）+ TypeScript + Tailwind；部署目标 Vercel
- 范围：**已定稿 = B** MVP 带轻后端（浏览、搜索、详情、阅读页）；C 的账号/收藏/评论/付费明确不在本期
- 决策记录：`docs/specs/决策记录.md`（用户点头过的决策都记在此）
- 文档位置：规格 `docs/specs/`、任务单 `docs/tasks/`、任务回报 `docs/reports/`

## 角色与唯一入口

**唯一入口是架构师会话**：用户只对架构师说话，架构师负责拆解与派发；其他角色不接受用户直接指派。

| 角色 | 章程 | 何时进场 |
| --- | --- | --- |
| 产品经理 | `docs/roles/产品经理.md` | 第 1 步：需求与验收标准 |
| 架构师 | `docs/roles/架构师.md` | 全程：调度台 + 架构决策 |
| 全栈工程师 | `docs/roles/全栈工程师.md` | 契约冻结后 |
| 测试工程师 | `docs/roles/测试工程师.md` | 与实现同期 |
| 运维工程师 | `docs/roles/运维工程师.md` | 首次可部署后 |

架构师有两条派发通道：

1. **子代理**（一次性任务：实现、评审、测试）——同一时刻最多 3 个并行，禁止两个代理改同一文件。
2. **角色会话**（长期角色：产品/全栈/测试/运维）——用于需要保留上下文的持续工作。**凡是已有同名角色会话的身份，一律投递到该会话**（`codex queue --thread "漫画站-<角色>" --message "..."`），不得用子代理替代；子代理只用于没有对应会话的任务（例如一次性 UI 设计）。

无论走哪条通道，任务都必须先落成 `docs/tasks/T-xxx.md`，消息里只给任务单路径与验收要点。

## 硬规则

1. **契约先行**：`docs/specs/design.md` 与 `docs/contracts/` 冻结后才允许并行开发。
2. **单写者**：`src/db/schema.ts`、`src/app/layout.tsx`、`src/app/globals.css`、`next.config.mjs` 同一时刻只允许一个角色修改。
3. **目录隔离**：并发开发各自使用 `.worktrees/<角色>-<特性>` 工作区（已加入 `.gitignore`）。
4. **证据优先**：任何「已完成」必须附命令与输出，记入 `docs/reports/T-xxx.md`。
5. **两阶段评审**：实现 → 规格评审（是否少做/多做）→ 质量评审，顺序不可颠倒。
6. **用户闸门**：设计定稿、计划定稿、合并上线，三处必须用户点头。

## 用户偏好（跨角色硬要求）

7. **学习导向注释**：用户正在学 Next.js。所有交付的代码要在关键步骤写**中文注释**，说明「这里在 Next.js 里是什么机制、为什么这么写、数据从哪来到哪去」；评论与评审时，注释质量算验收项。注意不要逐行复述语法，注释要解释机制而非噪音。

## 命令（工程创建后以 package.json 为准）

`pnpm dev` / `pnpm build` / `pnpm test` / `pnpm e2e` / `pnpm type-check` / `pnpm lint`

