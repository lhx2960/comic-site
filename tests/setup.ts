/* =============================================================================
 * Vitest 全局启动文件
 *
 * 只做一件事：把 jest-dom 的 DOM 断言（toBeVisible、toHaveAccessibleName 等）
 * 注册进 Vitest 的 expect，供 T-004 之后的组件测试使用。纯逻辑测试不受影响。
 * ========================================================================== */

import "@testing-library/jest-dom/vitest";
