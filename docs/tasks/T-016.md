# T-016 测试收口：F-07 断言改写 + F-06 回归用例 + 生产构建全量复跑

| 字段 | 内容 |
| --- | --- |
| 任务号 | T-016 |
| 承接角色 | 测试工程师（会话「漫画站-测试」） |
| 依赖 | T-013（已切生产构建）、T-015（F-06 修复） |
| 可改目录 | `e2e/**`、`playwright.config.ts`（仅注释/超时类微调）、`docs/reports/T-016-验证.md` |
| 交付格式 | 测试改动 + 全量复跑证据 + 回报 |
| 状态 | **已完成** —— 承接方：会话「漫画站-测试」；提交 `374d2ed`；结果：F-06 回归 20×3 = 100/100、F5-4b 改写通过、prod 全量两轮 106/107 → 107/107；唯一红灯 `D-016-②` 经定性为测试过度断言，已开 T-017（D-022） |

## 背景

T-013 在生产构建上暴露两类红灯，架构师已裁决：

- **F-06（真实缺陷）**：详情页预取在途时点击话条目偶发不跳转 → **D-020**：`ChapterList` 的链接关闭预取，由 **T-015** 修复。测试侧要求：把它变成**正式回归用例**（T-013 里那段临时复现脚本不能只留在报告里）。
- **F-07（测试断言过时）**：`F5-4b` 的中间断言「话末出现『下一话 · 第 4 话』区块」在 T-011 之后变成竞态（接续只需 25–37ms，区块一闪就被第 4 话内容顶掉）。**架构师授权改写这条断言**（见下）。

## 要做的事

1. **改写 `F5-4b` 为回归用例**（授权范围仅限这一条）：
   - **删除**「必须先看到『下一话 · 第 4 话』区块」这句中间断言（它描述的是过渡态，不是需求）
   - **保留并强化**实质断言：一步滚到底并停住后，`[data-section="4"]` 在合理时限内出现、顶栏与地址栏都变成第 4 话、无控制台错误
   - 在用例名与注释里说明它回归的是 **F-01**（T-011 修的那个缺陷）
2. **新增 F-06 正式回归用例**（例如 `e2e/navigation-race.spec.ts`）：
   - 复刻 T-013 §5.1 的复现路径：进入详情页 → 首屏未完全稳定时点击某一话 → 断言最终地址与页面内容都变为该话（**允许重试点击不算通过**：必须在一次点击后成立）
   - 用 `--repeat-each=20` 级别的重复度或 3 worker 并发方式跑一次，贴出「0 失败」的输出
3. **在生产构建上跑全量** `pnpm e2e`，目标**全绿**（用例数可能因新增用例而增加，把总数写清楚）
4. 报告里给出「验收标准 → 用例 → 结果」的增量更新（相对 T-009/T-013），以及本轮与 T-013 的差异

## 验收标准

- [ ] `F5-4b` 已改写为回归用例，实质断言未减少（只删了描述过渡态的那一句）
- [ ] F-06 回归用例存在，并在**修复后**的代码上 20 次重复 / 3 worker 全过（附原始输出）
- [ ] `pnpm e2e` 在生产构建下**全绿**（附完整尾部输出与总数）
- [ ] `git diff` 证明除上述断言改写外，**未放宽任何其它断言、未跳过任何用例**
- [ ] 未改 `src/**`、未改契约

## 约束

- 授权改写**只限 `F5-4b` 的中间断言**；其它任何红灯都要保留并回报架构师
- 不改 `src/**`；不要开子代理；完成后落盘 `docs/reports/T-016-验证.md`
- 若 T-015 的提交尚未出现，先完成测试改动，然后每隔约 2 分钟检查一次 `git log`（最多等 20 分钟），拿到修复后再跑全量

## 回报要求

状态 + 证据（命令与输出）+ 改动文件 + 验收逐条核对 + 遗留问题。
