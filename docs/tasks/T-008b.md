# T-008b 收尾补丁：逐作品 404 文案 + 移动端滚动约束 + 站头提取

| 字段 | 内容 |
| --- | --- |
| 任务号 | T-008b |
| 承接角色 | 全栈工程师（会话「漫画站-全栈」） |
| 依赖 | T-008 |
| 可改目录 | `src/app/comics/[slug]/not-found.tsx`（新建）、`src/app/comics/[slug]/[chapter]/not-found.tsx`（新建）、`src/app/globals.css`（**只新增滚动约束那一行**）、`src/app/page.tsx`、`src/app/comics/[slug]/page.tsx`、`src/app/comics/[slug]/[chapter]/page.tsx`（**只把站头替换成 SiteHeader**）、`src/components/SiteHeader.tsx`（新建）、`tests/components/**` |
| 交付格式 | 代码 + 测试 + `docs/reports/T-008b.md` |
| 状态 | **已完成** —— 承接方：会话「漫画站-全栈」；提交 `71989bf`（逐作品 404 文案 + 滚动约束 + SiteHeader 提取与无障碍补强） |

## 背景

T-008 的三条遗留（回报里的遗留 2、3、5）都因为跨文件而无处落地，本补丁一次做完，之后才进入 E2E 验收（T-009），避免测试跑在还会变的代码上。

## 要做的事

1. **逐作品 404 文案**（PRD F4-5、F5-9、`ui.md` §5.3）：
   - `src/app/comics/[slug]/not-found.tsx`：「这部漫画不存在」+ 说明 + 「返回首页」与「去漫画库」入口
   - `src/app/comics/[slug]/[chapter]/not-found.tsx`：「这一话不存在」+ 说明 + 「返回详情」与「返回首页」入口
   - 两个文件都要能被 `notFound()` 命中（必要时用 `layout.tsx` 或页面内的 `notFound()` 触发；**不要**改路由结构）
2. **移动端滚动约束**：`src/app/globals.css` 里只加一行 `body { overscroll-behavior-y: contain }`（写在 `html`/`body` 上才有效），除此之外不改 `globals.css`
3. **站头提取**（D-014）：新建 `src/components/SiteHeader.tsx`（站点名 + 返回首页入口），替换首页、详情页、阅读页各自的站头标记；**视觉与语义保持不变**（含 `aria-current`、焦点样式、触控尺寸）
4. 单测：两个 not-found 组件的文案与出口、`SiteHeader` 的链接与无障碍

## 验收标准

- [ ] `pnpm type-check` / `lint` / `test` / `build` 全绿（附输出）
- [ ] 生产服务器实测：`/comics/nope` 页面出现「这部漫画不存在」+ 返回入口；`/comics/xinghai/9` 页面出现「这一话不存在」+ 返回详情
- [ ] `globals.css` 的 diff 只有一行（用 `git diff --stat` 佐证）
- [ ] 三个页面的站头由同一个 `SiteHeader` 渲染（`rg -n "SiteHeader" src/app` 命中 3 处）
- [ ] 阅读页的深色域、详情页的吸底 CTA 等既有行为不受影响（截图或断言）

## 约束

- 不做状态架构改造（D-015）、不建 `copy.ts`（D-014）、不改数据层与契约
- 只改「可改目录」列出的路径；发现需要更大改动时回报架构师
- 不要开子代理；完成后落盘 `docs/reports/T-008b.md`

## 回报要求

状态 + 证据（命令与输出）+ 改动文件 + 验收逐条核对 + 遗留问题。
