# T-016 验证回报：F-06 回归锁死（100/100）、F5-4b 改写通过、全量 107/107；另有一条越界红灯待裁决

| 字段 | 内容 |
| --- | --- |
| 任务号 | T-016 |
| 承接角色 | 测试工程师（会话「漫画站-测试」） |
| 状态 | **DONE_WITH_CONCERNS**（F-06 已用正式回归用例钉死；全量可达 107/107，但 `D-016-②` 有一条**超出我授权范围**的偶发红灯，见 §5） |
| 验证对象 | 生产构建产物（`pnpm build` + `next start -p 3100`），修复基线 `b454a44`（T-015）+ `59b63ca`（D-021） |
| 统计 | 全量 **107 passed**（第 2 轮全绿；第 1 轮 106/107，唯一红 = `D-016-②` 偶发）<br>F-06 正式回归 **20 次重复 × 3 worker = 100 passed / 0 failed** |
| 授权范围 | 只改写了 `F5-4b` 的中间断言（§1.1）；其它断言一行未动（§7） |

---

## 1. 测试侧的改动

### 1.1 `F5-4b` 改写为 F-01 回归用例（T-016 唯一授权的断言改动）

改动前（T-009 留痕版，原文）：

```ts
test("F5-4b【缺陷 F-01】直接跳到底部并停住时，第 4 话应当自动接续", async ({ page }) => {
  await page.goto("/comics/xinghai/3");
  await expect(page.locator('img[alt="第 3 话 第 1 页"]')).toBeVisible();
  await page.waitForTimeout(500);
  await scrollToDocumentBottom(page);
  // 预取已经成功、话末区块已显示第 4 话标题，但内容始终没有接上
  await expect(page.getByRole("button", { name: /下一话 · 第 4 话/ })).toBeVisible();
  await expect
    .poll(() => page.locator('[data-section="4"]').count(), { timeout: 8000 })
    .toBeGreaterThan(0);
});
```

改动后（本轮）：

```ts
test("F5-4b 回归（F-01）一步滚到底并停住后，第 4 话自动接续、顶栏与地址栏同步", async ({ page }) => {
  const consoleErrors: string[] = [];          // 新增：收集 console error / pageerror
  page.on("console", (message) => { if (message.type() === "error") consoleErrors.push(message.text()); });
  page.on("pageerror", (error) => consoleErrors.push(`pageerror:${error.message}`));

  await page.goto("/comics/xinghai/3");
  await expect(page.locator('img[alt="第 3 话 第 1 页"]')).toBeVisible();
  await page.waitForTimeout(500);
  await scrollToDocumentBottom(page);          // 一步到底（等价：按 End / 拖滚动条到底 / 甩到底）

  // F-01 的实质：数据后到、滚动已停，第 4 话仍必须自己接上来
  await expect(page.locator('[data-section="4"]')).toBeAttached();   // 合理时限（expect 默认 15s）
  await expect(sectionBlocks(page, 4)).toHaveCount(10);
  // 继续往下读第 4 话：顶栏与地址栏同步（F5-4 的同步要求）
  await sectionBlocks(page, 4).first().evaluate((el) => el.scrollIntoView({ block: "start" }));
  await expect(readerHeader(page)).toContainText("第 4 话");
  await expect.poll(() => page.url()).toMatch(/\/comics\/xinghai\/4$/);
  expect(consoleErrors, "接续过程不应产生控制台错误").toEqual([]);
});
```

删掉的只有「必须先看到『下一话 · 第 4 话』区块」这一句（它描述的是修复前的过渡态）；
实质断言一条没少，另外**新增**「第 4 话 10 个页块齐备」「顶栏与地址栏同步为第 4 话」「无控制台错误」三条。
用例名与注释都写明它回归的是 **F-01**（T-011 修的那个缺陷）。

**为什么「顶栏/地址栏同步」放在继续往下读之后**：标签跟的是「当前读到哪」——一步到底并停住时
用户正看着第 3 话末尾，此时标签理应还是第 3 话；等继续滚进第 4 话正文，标签与地址才同步为第 4 话。
这是 F5-4 的原意，也正是 D-016 那三条在验的东西。

### 1.2 新增 `e2e/navigation-race.spec.ts`（F-06 正式回归，5 条）

| 用例 | 钉住的语义 |
| --- | --- |
| F-06 回归：详情页首屏未稳定时点「第 1 / 3 / 5 话」，**一次点击**即进入阅读页（3 条，覆盖三部作品） | 复刻 T-013 §5.1 的复现路径：`goto(详情页)` 后**不加任何等待**立刻点话条目 → 地址、内容、顶栏三处都必须是该话；全程无控制台错误。**失败时不补点**（补点等于把缺陷藏起来） |
| F-06 回归（机制 / D-021）：详情页首屏不发任何指向某一话的预取请求 | 直接钉 D-021 的机制：谁把 `prefetch={false}` 改回去（或升级 Next 后行为变化），这条立刻红 |
| F-06 同机理观察（首页卡片仍保留预取）：一次点击即进入详情页 | D-021 的「待观察」项，用同样的「只点一次」纪律盯着首页卡片路径 |

---

## 2. 证据：F-06 回归 20 次重复 × 3 worker（生产构建）

```
> pnpm exec playwright test e2e/navigation-race.spec.ts --repeat-each=20 --workers=3 --reporter=line

Running 100 tests using 3 workers
  100 passed (1.2m)
```

**0 失败**，且这 100 次全部是「一次点击就成立」——用例里没有任何重试点击、没有点击前的等待。
对照 T-013：修复前同一条路径（临时复现脚本）基线 1/20～2/20 命中；本次 0/100。

单独复跑（取报告要贴的原始行）：

```
> pnpm exec playwright test -g "F5-4b|F-06 回归" --reporter=list
  ok 1 [chromium] › navigation-race.spec.ts:36 › F-06 回归：详情页首屏未稳定时点「第 1 话」，一次点击即进入阅读页 (624ms)
  ok 2 [chromium] › navigation-race.spec.ts:36 › F-06 回归：详情页首屏未稳定时点「第 5 话」，一次点击即进入阅读页 (658ms)
  ok 3 [chromium] › navigation-race.spec.ts:36 › F-06 回归：详情页首屏未稳定时点「第 3 话」，一次点击即进入阅读页 (669ms)
  ok 4 [chromium] › navigation-race.spec.ts:65 › F-06 回归（机制 / D-021）：详情页首屏不发任何指向某一话的预取请求 (1.8s)
  ok 5 [chromium] › reader.spec.ts:269 › D-016 接下一话的地址同步 › F5-4b 回归（F-01）一步滚到底并停住后，第 4 话自动接续、顶栏与地址栏同步 (938ms)
  5 passed (28.0s)

> pnpm exec playwright test -g "同机理观察" --reporter=list
  ok 1 [chromium] › navigation-race.spec.ts:84 › F-06 同机理观察（首页卡片仍保留预取）：一次点击即进入详情页 (751ms)
  1 passed (26.0s)
```

---

## 3. 证据：生产构建全量复跑

```
> pnpm e2e
[WebServer]  ✓ Compiled successfully in 2.3s
[WebServer]  ✓ Generating static pages (4/4)
[WebServer]  ✓ Ready in 527ms
Running 107 tests using 3 workers
  1 failed
  106 passed (1.3m)          ← 第 1 轮：唯一红 = D-016-②（§5）
```

```
> pnpm e2e
Running 107 tests using 3 workers
  107 passed (1.2m)          ← 第 2 轮：全绿
```

用例总数 **107**（Chromium 80 + WebKit 27）：T-013 的 102 条 + 本次新增 5 条（`navigation-race.spec.ts`）。

---

## 4. 「验收标准 → 用例 → 结果」增量更新（相对 T-009 / T-013）

| AC / 缺陷 | 用例 | T-009（dev） | T-013（prod） | **T-016（prod）** |
| --- | --- | --- | --- | --- |
| F5-4（接下一话主路径） | `reader` F5-4 | ✅ | ✅ | ✅ |
| **F-01**（一步到底不接续） | `reader` **F5-4b 回归（F-01）**（本轮改写） | ❌ 真缺陷 | ❌ 中间断言竞态（F-07） | ✅ **通过**（实质断言保留 + 强化） |
| **F-06**（点一次不跳转） | `navigation-race` 3 条行为 + 1 条机制 + 1 条首页卡片观察（本轮新增） | 未发现 | ❌ 每轮 3～5 条同类红灯 | ✅ 5/5 通过；20×3 = **100/100** |
| D-016-①/②/③（地址同步） | `reader` D-016-①/②/③ | ✅ | ①/③ 通过、② 因 F-06 红 | ①/③ ✅；**② 偶发红**（20 块，见 §5） |
| F5-9（这一话不存在） | `reader` F5-9 | ✅（dev 行为） | ✅（T-012 修后） | ✅ |
| F5-10 / F5-12（懒加载 / 无跳动） | `reader-lazy` | ✅ | ✅ | ✅ |
| F4-4 / F4-6 / F4-7 / F5-7（详情页 → 阅读页各入口） | `detail` / `reader` 对应用例 | ✅ | ❌ 被 F-06 拖红 | ✅（D-021 关掉详情页全部预取后不再复现） |
| F6-1 ～ F6-7、F1/F2/F3、G1～G10 | 同 T-009 映射表 | ✅ | 除 F-06 波及项外 ✅ | ✅（全量第 2 轮 107/107） |

---

## 5. 唯一红灯：`D-016-②` 的越界断言（需架构师裁决）

**现象**（第 1 轮全量）：`D-016-② 滚到第 4 话后硬刷新，仍落在第 4 话` 在最后一句
`await expect(pageBlocks(page)).toHaveCount(10)` 失败：

```
Error: expect(locator).toHaveCount(expected) failed
Locator:  locator('[data-page-index]')
Expected: 10
Received: 20
Timeout:  15000ms
```

失败现场快照显示：顶栏 `第 4 话 · 日志第七页`、URL `/comics/xinghai/4`，
但页面里多出 `region "第 5 话 回信"` —— **硬刷新后第 5 话被自动接上了**（20 = 第 4 话 10 页 + 第 5 话 10 页）。

**诊断（一次性脚本，已删除）**：

```
BEFORE-RELOAD record = null                       ← 500ms 防抖还没落盘，没有本机进度可恢复
BEFORE-RELOAD state  = {"url":"/comics/xinghai/4","scrollY":9735,"scrollHeight":19467,"pageBlocks":20,...}
AFTER-RELOAD  record = {"xinghai":{"chapter":4,"page":10,"finished":false,...}}
AFTER-RELOAD  state  = {"url":"/comics/xinghai/4","scrollY":9069,"scrollHeight":9789,"pageBlocks":10,...}
```

原因链：刷新前文档是 20 块（第 3+4 话）、滚动位置在 9700+px；`page.reload()` 后浏览器会把滚动偏移
**恢复到新文档**，而新文档只有第 4 话（9789px），偏移被夹到 9069px ≈ 第 4 话末尾 → 阅读器判定
「剩余不足一屏」→ 预取并接上第 5 话 → `[data-page-index]` 变成 20。

**判定**：`D-016-②` 的实质「硬刷新后仍落在第 4 话」**通过了**（URL + 顶栏都是第 4 话）；
失败的是用例里额外加的「页面里只能有 10 个页块」——它描述的是「刷新后不会自动接下一话」，
而这正是 F5-4 / D-021 明确要有的行为。**属于测试的过度断言，不是产品缺陷。**

**复现率**（隔离跑，生产构建）：

```
> pnpm exec playwright test -g "D-016-②" --project=chromium --workers=3 --repeat-each=10 --reporter=line
  2 failed
  8 passed (57.0s)          ← 2/10 命中，失败签名固定为「20 blocks instead of 10」
```

**为什么不在本轮直接改**：这是 `F5-4b` 之外的断言，超出你给的授权范围（「其它红灯一律保留回报我」），
所以保持原样并交回裁决。若追认，最小改法是二选一：

```ts
// 方案 A（推荐）：只数第 4 话自己的页块
await expect(sectionBlocks(page, 4)).toHaveCount(10);
// 方案 B：不限制是否已接上第 5 话，只保证本话页块齐备
await expect(sectionBlocks(page, 4)).toHaveCount(10);   // 语义同 A，去掉全局计数
```

### 5.1 附带观察（不判为缺陷，仅记录）

- 刷新前若 500ms 防抖没落盘（例如刚点进某话就立刻刷新），本机进度为空 → 阅读器不做应用级恢复，
  只能依赖浏览器的滚动恢复，落点可能偏到本话末尾（上例就是第 4 话第 10 页）；随后写入的记录是
  「第 4 话第 10 页」，**与用户刷新后实际看到的位置一致**，没有自相矛盾。
- 若进度已落盘，应用级恢复（`scrollIntoView` 到记录页）会覆盖浏览器的滚动恢复，落点即记录页。

---

## 6. 与 T-013 的差异

| 维度 | T-013（prod，修复前） | T-016（prod，T-015 + D-021 之后） |
| --- | --- | --- |
| F-06 导航竞态 | 每轮全量 3～5 条红灯（F4-4/F4-6/F4-7/F5-7/D-016-②/③） | 详情页所有链接已 `prefetch={false}`；正式回归 20×3 = **100/100**，全量两轮无该类红灯 |
| F-07 / F5-4b | 中间断言竞态，红 | 已按授权改写为 F-01 回归用例，**通过** |
| F-06 的复现路径 | 只留在报告 §5.1 的临时脚本 | 固化成 `e2e/navigation-race.spec.ts`（4 条回归 + 1 条首页卡片观察） |
| 用例总数 | 102 | **107** |
| 全量结果 | 99 / 96 / 97 | **107/107**（第 2 轮）；第 1 轮 106/107（§5 的越界断言） |
| 待观察项 | — | 首页卡片路径（D-021「待观察」）已加用例，本轮通过 |

---

## 7. 改动文件与「未放宽其它断言」证明

```powershell
> git status --short
 M e2e/reader.spec.ts
?? e2e/navigation-race.spec.ts

> git diff --stat -- e2e
 e2e/reader.spec.ts | 48 +++++++++++++++++++++++++++++++++++--------------
 1 file changed, 34 insertions(+), 14 deletions(-)
```

- 被修改的只有 `e2e/reader.spec.ts`，且**全部落在 `F5-4b` 这一个用例体内**（§1.1 的前后对照）；
  其余 spec 文件字节未变（`git status` 里没有它们）。
- 新增一个文件 `e2e/navigation-race.spec.ts`（只增用例，不改既有用例）。
- 未改 `src/**`、未改 `docs/contracts/**`、未改 `playwright.config.ts`、未改数据层。
- 诊断用的 `e2e/tmp-debug.spec.ts` 已删除，不在提交里。

---

## 8. 验收标准逐条核对

| 验收项 | 结果 |
| --- | --- |
| `F5-4b` 已改写为回归用例，实质断言未减少（只删了描述过渡态的那一句） | ✅ §1.1：删 1 句过渡态断言，新增 3 条实质断言（页块齐备 / 顶栏与地址栏同步 / 无控制台错误） |
| F-06 回归用例存在，并在修复后代码上 20 次重复 / 3 worker 全过（附原始输出） | ✅ §2：`100 passed (1.2m)`，0 失败；行为用例均为「一次点击就成立」，无重试点击 |
| `pnpm e2e` 在生产构建下全绿（附完整尾部输出与总数） | ✅ **107 passed（共 107 条）** §3 第 2 轮；第 1 轮 106/107 的唯一红是 `D-016-②` 的越界断言（§5，已回报、未擅自修改） |
| `git diff` 证明除上述断言改写外，未放宽任何其它断言、未跳过任何用例 | ✅ §7 |
| 未改 `src/**`、未改契约 | ✅ §7 |

---

## 9. 遗留与建议

1. **`D-016-②` 的越界断言等你裁决**（§5）：2/10 命中、签名固定；实质 AC 通过，建议按方案 A 收窄。
   在你点头前我不会动它，全量因此会在 106/107 与 107/107 之间偶发摆动。
2. **首页卡片路径**（D-021「待观察」）已有 1 条用例盯着；本轮 prod 全量 2 轮均通过。
   若后续在真实慢网 / 长列表下复现，按 D-021 的同一条规则处理（关该路径预取）。
3. **建议把 `pnpm e2e` 作为交付前的固定门禁**：现在跑的是生产构建、107 条、两轮内全绿，
   唯一已知抖动是 §5 那条越界断言；修掉之后即可考虑打开 CI 的 e2e job（记得同步 `playwright-report/` 路径，见 T-013 报告 F-04）。
4. **T-009 的映射表已过期**（那版结论基于 dev）；本文件 §4 是增量更新，建议最终收口时以 T-016 的 prod 结论为准。

---

## 10. 提交

- 修复基线：`b454a44`（T-015 `fix`）+ `59b63ca`（D-021 文档）
- 提交信息：`test(T-016): F5-4b 改写为 F-01 回归 + 新增 F-06 正式回归用例，prod 全量 107/107`
- 提交范围：`e2e/reader.spec.ts`、`e2e/navigation-race.spec.ts`、`docs/reports/T-016-验证.md`
