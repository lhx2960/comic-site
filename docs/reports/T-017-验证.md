# T-017 验证回报：D-016-② 断言已按 D-022 收窄，10×3 与两轮生产构建全量均全绿

| 字段 | 内容 |
| --- | --- |
| 任务号 | T-017 |
| 承接角色 | 测试工程师（会话「漫画站-测试」） |
| 状态 | **DONE**（授权范围内的一处断言收窄已完成；两轮 prod 全量 107/107） |
| 验证对象 | 生产构建产物（`pnpm build` + `next start -p 3100`），基线 `c691d4f`（含 D-022） |
| 统计 | `D-016-②` 10 次重复 × 3 worker = **10 passed / 0 failed**；`pnpm e2e` **两轮均 107 passed**（共 107 条用例） |

---

## 1. 改动（授权范围：`D-016-②` 的最后一句）

```diff
--- a/e2e/reader.spec.ts
+++ b/e2e/reader.spec.ts
@@ -242,7 +242,11 @@ test.describe("D-016 接下一话的地址同步", () => {
     await expect(page).toHaveURL(/\/comics\/xinghai\/4$/);
     await expect(readerHeader(page)).toContainText("第 4 话");
     await expect(page.locator('[data-section="4"]')).toBeAttached();
-    await expect(pageBlocks(page)).toHaveCount(10);
+    // D-022 收窄：只数第 4 话自己的页块（原为全页 [data-page-index] === 10）。
+    // 硬刷新时浏览器会把刷新前的滚动偏移恢复到新文档，落点常常已经在本话末尾，
+    // 阅读器于是按 F5-4 的设计自动接上第 5 话 —— 全页会有 20 个页块，那是正确行为，
+    // 不能当失败判据（收窄前隔离复跑 2/10 命中，而失败现场 URL 与顶栏都正确）。
+    await expect(sectionBlocks(page, 4)).toHaveCount(10);
   });
```

- **只动了这一句**（外加解释性注释）：把「全页 `[data-page-index]` 必须等于 10」收窄为
  「第 4 话自己的页块等于 10」。前后语义差别：不再把「刷新后自动接上第 5 话」当成失败，
  而那正是 F5-4 / D-021 要求的行为。
- `pageBlocks()` 在其它用例里仍在使用（F5-1 / F5-3 / F5-4 / F5-7 / F5-8 / F6-1 等），没有被删掉或改写。

---

## 2. 证据：`D-016-②` 10 次重复 × 3 worker（生产构建）

```
> pnpm exec playwright test -g "D-016-②" --project=chromium --repeat-each=10 --workers=3 --reporter=line

Running 10 tests using 3 workers
  10 passed (46.3s)
```

**0 失败**（收窄前同一命令是 2 failed / 8 passed，失败签名固定为 `Expected 10 / Received 20`）。

---

## 3. 证据：生产构建两轮全量

```
> pnpm e2e
[WebServer]  ✓ Ready in 543ms
Running 107 tests using 3 workers
  107 passed (1.2m)          ← 第 1 轮
```

```
> pnpm e2e
[WebServer]  ✓ Ready in 537ms
Running 107 tests using 3 workers
  107 passed (1.2m)          ← 第 2 轮
```

两轮都是 **107 passed**（用例总数 **107** = Chromium 80 + WebKit 27，与 T-016 一致）。
每轮的产物整包归档在 `%TEMP%\comic-site-e2e-archive\t017-r1` / `t017-r2`（本轮无失败现场需要保留）。

**其它红灯**：本轮两轮全量里没有出现新红灯；T-016 报告 §5.2 记录的
`G6 可点元素都是原生 <a>/<button>…`（webkit）孤例**未再复现**（保持原样，未改该用例）。

---

## 4. 「未动其它断言」证明

```powershell
> git diff --stat
 e2e/reader.spec.ts | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

> git status --short
 M e2e/reader.spec.ts
?? "docs/reports/T-017-\351\252\214\350\257\201.md"
```

- 改动只落在 `e2e/reader.spec.ts` 的 `D-016-②` 一个用例体内（`git diff` 见 §1）；
- 未改 `src/**`、未改 `docs/contracts/**`、未改 `playwright.config.ts`、未改数据层；
- 未改其它 spec 文件（`git status` 里没有它们），未新增/删除任何用例（总数仍是 107）。

---

## 5. 验收标准逐条核对

| 验收项 | 结果 |
| --- | --- |
| `D-016-②` 改为按话计数，且该用例在 10 次重复跑里 0 失败（附输出） | ✅ §1 + §2：`10 passed (46.3s)` |
| 生产构建上 2 轮全量均全绿（附两轮原始输出） | ✅ §3：两轮各 `107 passed (1.2m)` |
| 改动只落在这一条用例内（`git diff` 证明） | ✅ §4：`1 file changed, 5 insertions(+), 1 deletion(-)` |
| 未改 `src/**`、未改契约、未改 `playwright.config.ts` | ✅ §4 |

---

## 6. 遗留与建议

1. **T-016 §5.2 的 `G6`（webkit）孤例仍在册**：本轮两轮 prod 全量未复现（累计 5 轮全量里只出现 1 次，
   隔离复跑 30×3 也没抓住）。现场（`trace.zip`）仍在
   `%TEMP%\comic-site-e2e-archive\r3\artifacts\global-G6-...-webkit-webkit\`。
   它的签名与 F-06 相同（导航 RSC 200 + 目标 chunk 200，但没提交），但被激活的链接自己已无预取，
   所以 D-021 的「关预取」不覆盖它。建议仍按 T-016 §9 的三个方向由架构师裁决，我不擅自动该用例。
2. **两处「测试过度断言」已经全部处理完**：F-07（F5-4b 中间断言，T-016 改写）、
   D-016-② 全页块数（本任务收窄）。之后的红灯若再出现，应优先按「先定性、再改」的流程走。
3. **建议把 `pnpm e2e` 作为交付前固定门禁**：现状 107 条、生产构建、连续多轮稳定全绿
   （T-016 的 5 轮 + 本任务 2 轮，共 7 轮里 6 轮全绿，唯一那次是 §5.2 的稀有孤例）；
   打开 CI 的 e2e job 前记得同步 `playwright-report/` 路径（T-013 报告 F-04）。

---

## 7. 提交

- 基线：`c691d4f`（D-022 + T-017 任务单）
- 提交信息：`test(T-017): 按 D-022 收窄 D-016-② 的全页块数断言，prod 两轮 107/107`
- 提交范围：`e2e/reader.spec.ts`、`docs/reports/T-017-验证.md`
