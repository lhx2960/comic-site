# T-001 回报：范围 B 的 PRD 与验收标准已落盘，6 项验收标准全部达成

| 字段 | 内容 |
| --- | --- |
| 任务号 | T-001 |
| 承接角色 | 产品经理（子代理 `/root/pm_prd/t001_pm`） |
| 状态 | DONE |
| 提交 | 未提交（工作区新增文件，由架构师决定提交时机）；基线 `66ad499d39aaa8336a40fa6f1b8bad2e491dc1a3` |

## 证据

### 1. 交付文件与指纹（`docs/specs/prd.md`）

```
Get-FileHash docs\specs\prd.md -Algorithm SHA256

Algorithm : SHA256
Hash      : F6D494519F76BD116B66B8DD2166B36994F14C5037E81EF59462534F9E1661E7

(Get-Content docs\specs\prd.md).Count
224
首行: # 漫画站 MVP（范围 B）产品需求文档（PRD）
末行: | v1.0 | 2026-09-25 | 初稿：按 D-001 ～ D-005 定稿范围 B 的功能、验收标准、不做清单与待确认清单 | 产品经理（T-001） |
```

### 2. 结构与覆盖（章节清单）

```
rg -n "^(#|##|###) " docs/specs/prd.md

1:# 漫画站 MVP（范围 B）产品需求文档（PRD）
18:## 1. 一句话目标
22:## 2. 目标用户与核心场景
34:## 3. 范围总览
58:## 4. 功能需求与验收标准
62:### F1 首页浏览（P0，依据 D-001、D-003）
74:### F2 漫画列表与标签筛选（P0，依据 D-001、D-005）
90:### F3 搜索：标题与作者（P0，依据 D-001、D-005）
105:### F4 漫画详情（P0，依据 D-001、D-003）
119:### F5 阅读页：纵向长条滚动 + 接下一话（P0，依据 D-001、D-004）
135:### F6 阅读进度记忆（P1，依据 D-004 配套项）
151:## 5. 全局要求
167:## 6. 本期明确不做
183:## 7. 术语
197:## 8. 待架构师确认（本 PRD 不自行拍板）
220:## 9. 变更记录
```

### 3. 验收标准条数

```
每功能 AC 数：F1 => 5，F2 => 9，F3 => 8，F4 => 7，F5 => 9，F6 => 7（合计 45 条）
全局要求：G1 ～ G9 共 9 条（含空状态 G2、错误状态 G3/G4、视口 G5、一致性 G8）
待架构师确认：14 条（Q1 ～ Q14）
```

### 4. 约束扫描：不出现技术方案（期望无输出，exit=1）

```
rg -n "Next\.js|React|Tailwind|Node|npm|pnpm|框架|目录结构|数据库|表结构|schema|接口|API|路由|组件|localStorage|缓存策略|工期|人日|估时|URL" docs/specs/prd.md
exit=1        # 无匹配
```

形容词扫描另有一处期望命中：第 14 行是「不出现『体验好』『速度快』这类形容词」这条规则本身的表述，非验收标准用语。

### 5. 「本期不做」覆盖校验

```
foreach($w in @('账号','收藏','评论','付费','后台管理','上传','真实作品入库')){ "$w => $(rg -c -- $w docs/specs/prd.md)" }

账号 => 4
收藏 => 1
评论 => 1
付费 => 1
后台管理 => 1
上传 => 2
真实作品入库 => 1
```

（计数为命中行数；对照 §6「本期明确不做」的七组清单。）

### 6. 工作区状态（只新增两个文件，未触碰其他文件）

```
git status --short
?? docs/reports/T-001.md
?? docs/specs/prd.md
```

## 改动的文件

- `docs/specs/prd.md`（新建，224 行）：范围 B 的目标用户与核心场景、F1 ～ F6 功能与 45 条可测试验收标准、G1 ～ G9 全局要求（空状态/错误状态/视口/一致性/文案）、本期不做清单、术语表、14 条待架构师确认、变更记录。
- `docs/reports/T-001.md`（新建）：本回报。

未改动：`AGENTS.md`、`docs/specs/决策记录.md`、`docs/tasks/`、`docs/contracts/`、`docs/roles/`。

## 验收标准核对

- [x] 覆盖首页浏览、列表 + 标签筛选、搜索（标题/作者）、详情（含话列表）、阅读页（长条滚动、接下一话、进度记忆）、全局空状态/错误状态 —— 依据：证据 2 的章节清单（F1 ～ F6 + G2/G3/G4）。
- [x] 每条功能都有可测试的验收标准（含输入与预期结果）—— 依据：证据 3 的 45 条 AC，格式统一为「输入/操作」+「预期结果」，含具体数值（3 部、5 话、10 页、第 3 话第 6 页等）。
- [x] 明确列出本期不做的事（账号、收藏、评论、付费、后台管理、上传、真实作品入库）—— 依据：证据 5 的关键词命中计数 + 文档 §6 七组不做清单。
- [x] 不出现技术方案（不选框架、不写目录结构、不写数据库表、不估工时）—— 依据：证据 4 的技术词扫描 exit=1（零命中）。
- [x] 不确定的产品问题集中在文末「待架构师确认」，不自行拍板 —— 依据：§8 的 Q1 ～ Q14，每条含「当前取值（暂用）+ 若不确认的影响」，并在 §4 用 «Q-x» 就地标注受影响 AC。
- [x] `docs/reports/T-001.md` 有证据、改动文件清单、验收标准逐条核对 —— 依据：本文件结构本身。

## 遗留问题与建议

1. **待架构师确认 14 条（Q1 ～ Q14）**：其中影响面最大的是 Q1（首页是否等同列表视图）、Q3（标签是否多选）、Q5（接下一话的形式与最后一话收尾）、Q6（阅读进度的粒度与清除入口）、Q12（图片级失败是否单独降级）。建议在写 `design.md` 前一次性答复，再据此微调对应 AC。
2. **示例数据的具体内容未定**：PRD 用「标签 A」「某作者」等占位表述，验收时需要以示例数据文件里的真实标题/作者/标签核对；建议架构师在任务单中要求数据满足 Q2、Q11 两条前提。
3. **D-002（学习导向中文注释）**：PRD 只登记不展开，需由 `design.md` 与各任务单的「交付格式」承接，否则后续评审没有检查依据。
4. **优先级建议**：F6 阅读进度记忆与 G6 键盘可达性标为 P1；若架构师在设计闸门认为进度记忆属于 MVP 核心（它同时决定 F4-6 的存在），建议上提为 P0 并同步修订 §3 范围总览。
5. **未提交**：本轮未执行 `git commit`（`docs/tasks/T-001.md` 的「可改目录」只授权上述两个文件，且 `.git` 对当前代理为只读），请架构师提交后再按 `BASE_SHA..HEAD_SHA` 派规格评审。
