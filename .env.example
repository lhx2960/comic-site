# =============================================================================
# 环境变量示例（本期为空）
#
# 用法：需要本地覆盖时才复制成 .env.local 再改；.env 与 .env.local 已在
# .gitignore 里，不会被提交。
#
# 本期（范围 B MVP）没有任何必需的环境变量：数据是仓库内的文件
# （data/comics.ts + public/comics/**），没有数据库、没有第三方服务、没有密钥。
# 所以这个文件是"空"的，是有意为之，不是漏写。
# 完整说明见 docs/ops.md「2. 环境变量清单」。
#
# 三条规矩（新增变量前先读一遍）：
#   1) 名字大写 + 下划线；只有 NEXT_PUBLIC_ 开头的会打包进浏览器端代码，
#      其余变量只在服务端（含 Route Handler）可见。
#   2) 真实值只写在两处：本机 .env.local，或 Vercel 的
#      Project Settings → Environment Variables。
#      任何密钥都不许出现在仓库、文档、任务单、回报或聊天记录里。
#   3) 新增变量时要同步更新 docs/ops.md 的「环境变量清单」表格，并在
#      Vercel 的 Preview 与 Production 两个环境都配好——只配一个是很常见的坑。
#
# 将来真要用变量时，照这个格式加（示例，当前请保持注释状态）：
# NEXT_PUBLIC_SITE_URL=https://example.vercel.app
#
# 另外，Vercel 会自己注入一批只读变量（VERCEL_ENV、VERCEL_URL、
# VERCEL_GIT_COMMIT_SHA 等），这些不需要也不应该手写在这里，运行时直接读即可。
